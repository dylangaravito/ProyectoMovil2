<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.garavito.proyectomovil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoMovil">
        <activity android:name=".NuevaDenuncia2"></activity>
        <activity android:name=".DenunciaGuardada" />
        <activity
            android:name=".NuevaDenuncia"
            android:label="@string/title_activity_nueva_denuncia"
            android:theme="@style/Theme.ProyectoMovil.NoActionBar" />
        <activity android:name=".Splash" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProyectoMovil.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>